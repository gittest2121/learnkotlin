fun sum(a: Int, b: Int): Int {
	/* two Int parameters
	 * Int return type
	 */

	return a+b;
	/*
	 * Semicolons are optional
	 * but anyway.
	 *
	 * So, line breaks are significant
	 */
}

fun main() {
	print("Sum of 7 and 9 is: ");
	println(sum(7, 9));
}
